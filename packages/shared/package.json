{
  "name": "@waylo/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Waylo — SSOT : types purs partagés backend ↔ mobile. ZÉRO dépendance runtime (tout est effacé au build).",
  "type": "commonjs",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./automation": {
      "types": "./src/automation/index.ts",
      "default": "./src/automation/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --config vitest.config.ts"
  }
}
